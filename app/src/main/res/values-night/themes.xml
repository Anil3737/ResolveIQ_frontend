<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Resolveiq_frontend" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/dark_surface</item>
        <item name="colorOnSurface">@color/on_surface_dark</item>
        <item name="android:colorBackground">@color/dark_background</item>
        <item name="colorOnBackground">@color/on_background_dark</item>
        <item name="colorOutline">@color/gray_500</item>
        
        <!-- Bottom Navigation Indicator Fix -->
        <item name="colorSecondaryContainer">#1E3A8A</item> <!-- Deep blue for dark mode active indicator -->
        <item name="colorOnSecondaryContainer">@color/white</item>
    </style>
</resources>